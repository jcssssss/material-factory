- 优化：是否选择多个文件夹呢？
- 新增：可选择哪些资料进行处理（转 jpg、资料列表、仿打印合成）
- 【未解决】资料为两页横版时该如何处理？
- 修复：libreoffice，无法启动应用程序。 配置文件「D:\xhs-tools\小红书素材工厂\_up_\vendor\libreoffice\program\bootstrap.ini」已经损坏。
- 无背景模板时，表单提示无背景模板，点击加入队列，强制引导去添加背景模板。不要报错：渲染异常：图片写入失败：E:\小红书虚拟\test/【旧版】2026八上英语_暑假衔接讲义_人教 pdf/专题02 名词、代词、形容词和情态动词（暑假复习讲义）（原卷版） /专题02 名词、代词、形容词和情态动词（暑假复习讲义）（原卷版） _p001.jpg - 写入图片失败：系统找不到指定的路径。 (os error 3)
- 表单里的[背景模板]改成【上传模板】
- 资料列表如果有“好评“类字眼文件夹，如何处理？
- 新增：任务队列列表新增打开文件夹功能
- 去水印

- 历史任务存在哪里？定时清理，超出固定提醒必须清理？一周，不超过 100 条，超过部分按时间最远的开始顶出栈。
- 日志存在哪里？日志定时清理，一周，不超过 10000 条，超过部分按时间最远的开始顶出栈。
- 背景模板，超出数量提醒清理？

- 将项目生成支持windows10及以上和macOS13及以上版本的安装包推送到 github 上。各种安装包区别？有哪些？留哪些？
- 检测项目是否同时支持 macOS 13及以上版本和 Windows 10及以上版本，若支持，将项目生成这两个的安装包推送到 github 上，若有不支持，则解决。

- 执行进度执行进度条每个 PDF 文件一个进度,当前执行进度，进度条要显示的是单个文件夹整个任务的进度，现在感觉像是单个文件夹里第一个文件的进度。
想重新设计“执行进度“这块，现在资料转换图片和图片合成掺合在一起了有点乱，设计的清晰些，你有什么想法吗？
两者分开，一个任务一个任务，然后先进行资料转换图片进度、进度详情信息，接着图片合成进度、进度详情信息。进度你有什么更好的方案将资料文件转图片一个任务队列，合成仿打印一个任务队列。

- 若HEIC 仍是明显瓶颈（每张走 sips 1-3 秒），可以考虑后续引入libheif-rs 在 Rust 内解码。

- 任务效率优化速度


- 去水印
❯ /plan 新增功能，去水印，暂时先放在左侧导航栏新增单独一个模块。功能想法：输入上传资料文件夹，暂时支持PDF、Word，后续Excel、ppt, 支持批量上传多个文件夹（一个文件夹对应一个商品），支持上传单个即多个PDF或word文件，默认文件夹。功能要求能自动检测并识别每个资料是否有水印、页眉、页脚，若有，则去除，若无则不做处理，简单提示无水印，输出格式、标题等内容与输入保持一致，仅去除水印、页眉、页脚。后续考虑将该功能嵌入到工作台模块的PDF转换流程前做预处理。聊一下你的想法。
后续在PDF转换流程前前新增去水印
  、页眉、页脚功能，要求能自动检测识别上传的每个 资料是否有水印、页眉、页脚，若有则进行去

  
- 执行队列时报以下错误错，定位问题并解决。[Error] [error] task=task_ms3d5icq_sc0aj9 pdf=/Users/shijichang/Rich/网盘资源/咨询工程师历年真题/经济/按年份真题（18-25年）/5.2024年咨询工程师（经济）真题解析.pdf page=2 渲染异常：PDF 渲染超时（60s），page=2
	append (logger.ts:37)
	pageError (logger.ts:70)
	(anonymous function) (taskRunner.ts:359)
	
- 生成的资料列表，名称中去掉序号

- 优化左侧导航的 icon，换成符合功能模块主题的icon。
- 测试并验证该项目兼容性要兼容 macOS 13 以上，Windows 10 以上系统。
- 桌面图片尺寸和其他图标大小不一致，要求设置标准尺寸和其他桌面应用的图标尺寸一致。


 历史任务列表中添加任务id，增加日志功能，同样通过跳转到日志页面，通过
  任务id筛选出对应任务日志信息。历史任务新增打开输出目录功能，点击即打
  开输出目录。任务队列列表同样添加该功能，只不过要在当前任务完成后显示
  查看的按钮 


  - 任务队列中的任务id列，固定宽度，宽度减少至当前宽度一半，显示不全的内容用省略号。
  - 任务队列中的PDF数量不对，重新审查算法。
  - 删除输出根目录下提示：“固定规格：3:4 竖版 · JPG 100% · 300 DPI · 按 {任务名}/{PDF 文件名}/{pdfBaseName}_p{页码三位}.jpg 组织“，改为在输出根目录后加一个“信息“icon，鼠标滑动到该 icon 弹出 tips的方式来展现。

  - 启动队列后，点击取消没有实现取消的功能。


- 选择图片后上传的时候也很慢，这个之前的问题，按照以下方案解决一下。[Pasted text #1 +17 lines] 
- 生成 mac 和 Windows 的安装包和安装压缩包，多大？优化
- 图片尺寸 调整至1242×1656

- 主图
- spec 那种 分析需求后生成 需求文档，技术架构文档，开发任务拆分文档，checklist spec tasks这种的用的是什么？
- 合成的图片并没有生成
- 性能优化：好一点了，但还需优化。仿打印图片生成时间很慢，导致桌面端很卡，解决并优化这个问题。
- 如果资料是横版，则从中间分割，一边一张图 （后续）

-pikepdf 未安装：改用 PyMuPDF 实现相同功能。PyMuPDF 的 delete_annot() 同样只操作
  PDF 结构，不修改内容流




  - fix: 当资料目录很多，生成资料列表一页展示不下时，可多页展示，即生成多个资料列表图片，调整输出的目录结构，将输出的资料列表图片单独放在一个文件夹中，命名为“资料列表“。
  - fix：00041《基础会计学》章节练习题.pdf ，导出为 jpg 后，第一页是空白的，解决这个问题
  - 日志列表加上表头，第一列的时间格式调整，示例“2026-07-31 15:22:32“
  - 任务队列列表中的“PDF数“列，改为页数，值为当前任务中要处理所有页数。
/re

  - 刚才我上传 16 个背景模板后，页面没有响应大概 5 秒钟左右后弹出二次确认预览弹窗，到预览弹窗后图片渲染很慢，最后点击上传背景模板后完全卡住了。以下是我的想法，或者你有更好的建议请反馈给我，上传图片之前要求新增一条建议图片大小不能超过5M，若上传的图片超过 5M 将大小压缩至 5M 以内，上传之后，将图片弹出的图片预览列表懒加载或者再次压缩至 1M 以内预览，选择一个最佳实践，放置加载图片时卡顿。对了，同时把背景模板列表一并进行处理，防止图片过大导致加载慢和页面卡顿的问题。


   检验libreoffice是否已经捆绑在项目里，检测整个资料文件转jpg流程是
  否有问题。  


  - 点击上传背景模板，在本地选择上传模板点击确定以后，有模板上传时不要卡在原地不动，前端界面先立即进入上传背景模板进度弹窗并显示列表及进度。
  - 优化上传速度，在保证质量和流程不出问题的情况下尽最大努力提升上传速度，可较少图片大小。我想的是，上传时不要使用以前的压缩了，直接将图片改为 1242*1656（3:4）,这样是否大大提升上传速度？如果你有更好的最佳实践方案可以推荐给我。
  - 修复上传背景模板进度弹窗进度条，完全不准确，按照所有模板
  进度显示对应进度条的样式。
  - 根据业务逻辑和你的审美优化背景模板列表页的交互和样式。
  - 分析并修复所有按钮暂停和取消按钮的处理逻辑。

  - 背景模板页可以分两部分，顶部和底部列表分别套一张 card 样式。

  给压缩也加并发限制（如同时压缩 2-3 张），需要的话告诉我。

  1.背景模板列表是否有唯一模板 id, 若无，则给背景模板加上 id 字段。
  2.工作台中的选择背景模板功能修改为一个按钮（选择模板），通过跳转到背景模板页面进行选择，跳转到背景模板页面后屏蔽所有操作按钮，只保留背景模板列表，勾选一个背景模板以对应要合成的资料，未标定背景模板标签灰色，不可选择，选择完后点击确定按钮跳转到工作台，携带背景模板预览小图及名称，后续该任务（即该商品资料）图片合成时，使用刚刚选择的那个背景模板进行合成。

  - “输入来源“改成加粗“单个商品“，“页码规则“、“前 N 页“改成加粗
  - 背景模板页，新增全选、清空功能（需二次确认）。删除模板的二次确认弹窗调整为当前 UI 组件的弹窗样式。
  - 修复：背景模板点进去没有图片。
   如果已经捆绑进应用，那么我开发时是否还需要安装 libreoffice？
  - “资料列表展示图“、“图片合成（仿打印效果）“，两个表单控件增加下边距，具体距离和其他控件间距统一。
  - 任务全部执行完后，应用持续占高内存，是否需要在执行完成后做一下处理？或者你有什么更好的方案解决该问题？
   26.2.5

  - 【不处理】分析是否有替代libreoffice的最佳实践，要求是否有 API 调用，或者什么其他用户无感执行任务队列，word转换时无需下载或频繁打开应用的最佳实践。
  - 优化：在执行队列时应用整体还是会卡顿，风扇呼呼转，仿打印生成也很慢。请优化整个任务执行过程中每个阶段的执行效率，并保证质量。要求执行任务队列的任何阶段都不允许出现用户能感知的卡顿，并且要在很大程度上优化提升任务队列执行的速度，同时确保任务的质量不出问题。
  - 优化：将word转pdf的执行进度，也放在整体进度中，放在 PDF 转换的进度前面，同样要有对应的日志信息。
  - P2  │ 仿打印多 Worker                        │ 速度（后续） │
  │░░░░░│░分片、阶段预扫描、pdf.js1渲染移 Worker │         
  
  - word转pdf，为什么一次转6个？有优化转换速度的最佳实践方案吗？
  - [Error] Unhandled Promise Rejection: dialog.confirm not allowed. Command not found
	(anonymous function) (user-script:6:1:292)
  - 没有递归查找输入的文件夹吧？嵌套在2层文件夹里的资料报错“[Error] [error] task=7212f700e2854967 文件夹中未找到任何 PDF 或 Word 文件
	append (logger.ts:39)
	taskError (logger.ts:63)
	(anonymous function) (taskRunner.ts:190)“

  - 检查执行任务后，任务队列里从第一个word转pdf很快，但第二个开始word转pdf变得很慢，找到根因，并解决这个问题
  - 在点击启动队列按钮后到执行任务开始前会几秒钟的空档没有任何反应，这块优化一下用户体验。
  在“执行进度“中加个任务准备中

  - 我发现个问题，npm run tauri:dev 刚打开应用时只有几十M，处理完一批资料后，打开活动监视器，发现开发模式下占用系统 5.83G 内存，找到根因，并解决这个问题

  - 任务名称有什么实际用途吗？若没有，是否可以去掉任务名称表单项，使用资料文件夹名称作为任务名称？
  - 任务执行word转pdf时，打开LibreOffice，我看活动监视器每次打开执行转换时，LibreOffice的CPU占用率达到了将近 90%+，找到根因并解决。
  - 任务执行到仿打印合成流程处理中时，该应用进程CPU占用率达到了 390%+，找到根因并解决。

  - 预扫描要提速，可选「Rust 用 lopdf 读页数」替代预扫描 getDocument（P2，不阻塞），方案可行性及风险点。

  - 后续优化输出的图片大小，控制在200kb以内，空间至少压缩50%。

  - 任务执行后xhs-pic Web Inspector (8970)进程占用内存 8G左右，分析根因，保证转换速度的情况下，优化内存问题。

  - 执行队列中的取消按钮，现在没有实际作用，加入取消逻辑。
  - 检查任务队列、执行任务队列中所有按钮功能是否完整，有的按钮操作后不进行实际语义的处理，如有该问题则修复问题。
  - 优化背景模板上传图片后，无反应、卡顿等性能问题，优化用户体验。
  - 背景模板页上方新增全选、取消、选择未标定按钮并完善功能。

  - 背景模板列表页样式根据你的审美进行优化。
  - 继续优化背景模板上传图片后，无反应、卡顿等性能问题，优化用户体验。选择完图片点你确定后立即跳转到进度页或者加个加载中的提示，否则没有反应用户不知道发生了什么。

  - [Warning] ⚠️ React Router Future Flag Warning: React Router will begin wrapping state updates in `React.startTransition` in v7. You can use the `v7_startTransition` future flag to opt-in early. For more information, see https://reactrouter.com/v6/upgrading/future#v7_starttransition. (react-router-dom.js, line 4427)
  - [Warning] ⚠️ React Router Future Flag Warning: Relative route resolution within Splat routes is changing in v7. You can use the `v7_relativeSplatPath` future flag to opt-in early. For more information, see https://reactrouter.com/v6/upgrading/future#v7_relativesplatpath. (react-router-dom.js, line 4427)

  - 新建任务表单中“请至少配置前 N 页或自定义页码之一“，间距与其他表单项统一。新建任务表单整体使用一个card样式包裹。

  - 历史任务列表页，任务名、任务 ID、输出目录列超出宽度隐藏
  - 优化所有复制icon，纯黑色不好看，设计一个你觉得合适的。
  - 日志列表页，范围列增加宽度，不要让文字换行。

  - 项目中所有列表样式按业务和你审美重新设计。
  - 项目中所有 tips 按业务和你审美设计一个，并且只要滑动到隐藏的目标立刻弹出tips，动画效果也按你审美设计。
  
  - 当每一个任务执行完成后，检查并清理所有用不到的且影响速度、卡顿、缓存、占用 CPU、占用内存影响性能等等的东西。所有任务完成后同样检查清理。且所有检查和清理同样不要影响性能。

  - 所有列表中隐藏的内容都可通过鼠标滑动后通过tips显示全部，现在滑动到目标后，tips弹出延迟太长了，缩短至立即显示。


  - 上传是否必须要求 3：4尺寸？

- 【已解决】Word 批量转换全部卡死/超时/无进度（"开始转换 36 个 Word 文件…"后无下文）：根因是捆绑的 **LibreOffice 26.2.5 dmg 残缺**——soffice 启动后卡在不可中断的内核状态（Metal 初始化），连 kill 都无效，每次转换堆积一个僵尸进程。已验证 **25.8.7 完整可用**（单文件/批量 6 个真实 .doc 转换均正常）。prepare-libreoffice.sh 默认版本已改为 25.8.7，vendor 已重新捆绑。**勿再升回 26.2.5**。遗留的 26.2.5 卡死进程（STAT=UE）只能重启系统清除，不影响新功能。

- 【已加固】偶发崩溃：某 .doc 触发 soffice UNO RuntimeException（`libc++abi: terminating due to uncaught exception of type com::sun::star::uno::RuntimeException`），批量转换中整批进程崩溃 → 同批未完成的好文件被误判失败。已修复：a) 每批使用独立 profile（`lo_profile_{task}_{uniq}`），soffice 崩溃不污染后续批次；b) 进程异常退出（非超时）时对未生成的文件逐个独立重试（独立子 profile `lo_retry_{uniq}`），隔离真正坏的文件。实测同 profile 连续 6 批 35 文件均正常，崩溃为偶发。
