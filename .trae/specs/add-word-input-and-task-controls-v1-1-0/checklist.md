- [x] 系统接受 .docx/.doc 文件作为任务输入，并先转 PDF 再复用 v1.0 链路
- [x] Word 与 PDF 混合输入时，输出目录仍按原文件名组织子目录，不混淆来源
- [x] LibreOffice 未安装时，含 Word 任务被阻止并显式提示，纯 PDF 任务不受影响
- [x] Word 转 PDF 失败时该 Word 视为失败 PDF 工作项，继续同任务其他文件
- [x] 任务状态机新增 `paused` 与 `cancelled`，状态迁移符合规则
- [x] 用户可对 `running` 任务执行暂停，任务在页或 PDF 边界停止并切换为 `paused`
- [x] 用户可对 `paused` 任务执行继续，从最近完成的 PDF 与页之后恢复执行
- [x] 用户可对 `running` 或 `paused` 任务执行取消，状态切换为 `cancelled` 且已导出文件保留
- [x] 暂停/继续/取消按钮按状态正确控制可用性
- [x] 系统在每个 PDF 完成后持久化 PDF 级进度断点
- [x] 应用启动时检测未完成任务并在工作台顶部展示恢复入口
- [x] 用户点击“继续”后从断点跳过已完成 PDF，从下一个 PDF 起始页继续执行
- [x] 用户点击“放弃”后任务标记为 `cancelled`，已导出文件与历史记录保留
- [x] 持久化结构向后兼容 v1.0 已保存的最近任务数据
- [x] v1.0 纯 PDF 串行队列与导出规格（3:4、JPG 100%、300 DPI）不受 v1.1.0 改动影响
- [x] 需求文档与技术架构文档已更新 v1.1.0 能力描述
